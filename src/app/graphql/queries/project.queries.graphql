query getProject($id: ID!) {
  getProject(id: $id) {
    id
    name
    createdAt
    updatedAt
  }
}

query getAllProjects {
  getAllProjects {
    id
    name
    createdAt
    updatedAt
  }
}

query getAllProjectsOfUser{
  getAllProjectsOfUser {
    id
    name
    createdAt
    updatedAt
  }
}